<div class="sidebar">

    <div class="sb-brand">
        <div class="brand-icon">
            <i class="bi bi-clipboard2-pulse" aria-hidden="true"></i>
        </div>
        <div>
            <div class="brand-name">SPAnamnese</div>
            <div class="brand-sub">Sistema médico</div>
        </div>
    </div>

    <div class="nav-scrollable">
        <nav class="nav flex-column">

            <div class="nav-section-label">Navegação</div>

            <NavLink class="nav-item" href="/anamnese">
                <i class="bi bi-clipboard2-fill nav-icon"></i>
                Anamnese
                <span class="active-bar"></span>
            </NavLink>

        </nav>
    </div>

    <div class="sb-footer">
        <div class="user-row">
            <div class="user-avatar">DR</div>
            <span class="user-name">Dr. Usuário</span>
            <a href="/logout" class="logout-btn" aria-label="Sair">
                <i class="bi bi-box-arrow-right" aria-hidden="true"></i>
            </a>
        </div>
    </div>

</div>

<style>
    /* Brand */
    .sb-brand {
        padding: 1.25rem 1.25rem 1rem;
        border-bottom: 0.5px solid #e0e0e0;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .brand-icon {
        width: 32px;
        height: 32px;
        border-radius: 8px;
        background-color: #E1F5EE;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        font-size: 16px;
        color: #1D9E75;
    }

    .brand-name {
        font-size: 14px;
        font-weight: 600;
        color: #1a1a1a;
        line-height: 1.2;
    }

    .brand-sub {
        font-size: 10px;
        color: #1D9E75;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        margin-top: 1px;
    }

    /* Nav scroll area */
    .nav-scrollable {
        flex: 1;
        overflow-y: auto;
        padding: 0.25rem 0;
    }

    /* Section label */
    .nav-section-label {
        font-size: 10px;
        color: #aaa;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        padding: 1.1rem 1.25rem 0.4rem;
    }

    /* Nav items */
    .nav-item {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 0.52rem 1rem;
        margin: 1px 0.6rem;
        border-radius: 6px;
        font-size: 13.5px;
        color: #555;
        text-decoration: none;
        transition: background 0.15s, color 0.15s;
    }

        .nav-item:hover {
            background-color: #E1F5EE;
            color: #0F6E56;
            text-decoration: none;
        }

            .nav-item:hover .nav-icon {
                color: #1D9E75;
            }

        /* Active via Blazor NavLink */
        .nav-item.active {
            background-color: #E1F5EE;
            color: #0F6E56;
        }

            .nav-item.active .nav-icon {
                color: #1D9E75;
            }

            .nav-item.active .active-bar {
                display: block;
            }

    /* Icon */
    .nav-icon {
        font-size: 17px;
        width: 20px;
        text-align: center;
        flex-shrink: 0;
        color: #aaa;
    }

    /* Active indicator bar */
    .active-bar {
        display: none;
        width: 3px;
        height: 18px;
        background-color: #1D9E75;
        border-radius: 2px;
        margin-left: auto;
        flex-shrink: 0;
    }

    /* Footer */
    .sb-footer {
        padding: 1rem 1.25rem;
        border-top: 0.5px solid #e0e0e0;
    }

    .user-row {
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .user-avatar {
        width: 28px;
        height: 28px;
        border-radius: 50%;
        background-color: #E1F5EE;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 11px;
        font-weight: 600;
        color: #0F6E56;
        flex-shrink: 0;
    }

    .user-name {
        font-size: 12.5px;
        color: #555;
        flex: 1;
    }

    .logout-btn {
        font-size: 17px;
        color: #aaa;
        background: none;
        border: none;
        padding: 0;
        display: flex;
        text-decoration: none;
        transition: color 0.15s;
    }

        .logout-btn:hover {
            color: #1D9E75;
        }

</style>